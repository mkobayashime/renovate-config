{
  "extends": ["config:base"],
  "rangeStrategy": "pin",
  "packageRules": [
    {
      "matchUpdateTypes": ["patch"],
      "groupName": "dependencies (patch)"
    },
    {
      "matchPackagePatterns": ["node"],
      "rangeStrategy": "replace"
    }
  ]
}
